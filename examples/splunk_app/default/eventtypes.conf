# =============================================================================
# Example Splunk eventtypes for ODCP testing
# =============================================================================

[failed_login]
search = (sourcetype=wineventlog EventCode=4625) OR (sourcetype=authentication action=failure)

[successful_login]
search = (sourcetype=wineventlog EventCode=4624) OR (sourcetype=authentication action=success)
